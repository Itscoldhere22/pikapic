[project]
name = "src"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hougen", email = "hougentan@gmail.com" }
]
requires-python = ">=3.11,<3.13"
dependencies = [
    "pandas>=3.0.5",
    "torch==2.11.0",
    "torchaudio==2.11.0",
    "torchvision==0.26.0",
]

[project.scripts]
src = "src:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
